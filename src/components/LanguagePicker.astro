---
import { languages } from "../i18n/ui";
import DropdownMenu from "./DropdownMenu";
import { LanguageIcon } from "./Icons";

const languageItems = Object.entries(languages).map(([lang, label]) => ({
  label: lang === "en" ? `${label}🗽` : `${label}🌮`,
  href: lang === "en" ? "/" : `/${lang}/`,
}));
---

<div class="cursor-pointer">
  <DropdownMenu items={languageItems} client:load>
    <LanguageIcon width={30} color="#fff" />
  </DropdownMenu>
</div>
